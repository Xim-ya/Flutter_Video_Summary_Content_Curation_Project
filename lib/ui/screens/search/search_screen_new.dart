import 'package:movie_curation/utilities/index.dart';

class SearchScreenNew extends BaseScreen<BaseViewModel> {
  const SearchScreenNew({Key? key}) : super(key: key);

  @override
  @override
  Widget buildScreen(BuildContext context) {
    return Container();
  }
}
