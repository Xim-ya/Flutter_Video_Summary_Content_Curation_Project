import 'package:flutter/material.dart';

// Search Screen For Mobile Device
class SearchScreenMobile extends StatelessWidget {
  const SearchScreenMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
