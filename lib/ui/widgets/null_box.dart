import 'package:flutter/material.dart';

class NullBox extends StatelessWidget {
  const NullBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
